//2. Write a program to print a given number without its last digit.
#include <stdio.h>

int main()
{
    int a,unit;
    printf("Enter the number:");
    scanf("%d",&a);
    printf("the unit digit is:");
    unit=a/10;
    printf("%d",unit);
    return 0;
}
