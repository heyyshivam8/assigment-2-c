//12. Assume price of 1 USD is INR 76.23. Write a program to take the amount in INR and 
convert it into USD.

#include <stdio.h>

int main()
{
    float usd=76.43;
    int inr;
    printf("TOTAL INR IS:");
    scanf("%d",&inr);
    usd=inr/usd;
    printf("TOTAL USD IS:-%f ",usd);
    return 0;
}
